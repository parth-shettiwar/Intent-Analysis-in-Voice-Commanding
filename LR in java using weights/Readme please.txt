This is the java code containg wights and biases from the LR tensorflow model.

The matrix and vector are made and multiplied to get the desired output.

No external package is being used in it currently.